To get gnome-keyring to work with alternative desktop environments,
the line starting with OnlyShowIn was removed from both desktop files.

To use alternative agents (ssh-agent and gpg-agent), Hidden=true was removed
from gnome-keyring-ssh.desktop
