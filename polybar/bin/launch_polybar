#!/usr/bin/env bash
primary=$(xrandr -q | grep primary | cut -d " " -f1)
echo "Primary monitor is $primary"
MONITOR=$primary polybar --reload eve > /tmp/polybar_eve.log 2> /tmp/polybar_eve.log </dev/null &
